# Numerical noise reference

*Generated by `scripts/measure_op_noise.py`; do not edit by hand.*
*Measured at commit `e4fd771` on `2026-07-06`.*

Every piecewise-linear op in `torchwright/ops/` is measured on one or more
named input distributions characterising its expected input ranges.
The canonical data lives in `docs/op_noise_data.json`; this file is
regenerated from that JSON by `make measure-noise`.

For commentary on the numbers — which are expected, which warrant investigation — see the hand-written `docs/numerical_noise_findings.md`.

## Summary

| Op | Machine | Module | Max abs error | Max rel error | Worst distribution |
| --- | --- | --- | --- | --- | --- |
| `abs` | relu | `torchwright.ops.relu.arithmetic_ops` | 0 | 0 | `abs_uniform_pm50` |
| `bool_all_true` | relu | `torchwright.ops.relu.logic_ops` | 0 | 0 | `bool_triple_signed` |
| `bool_any_true` | relu | `torchwright.ops.relu.logic_ops` | 0 | 0 | `bool_triple_signed` |
| `bool_not` | relu | `torchwright.ops.relu.logic_ops` | 0 | 0 | `bool_single_signed` |
| `ceil_int` | relu | `torchwright.ops.relu.arithmetic_ops` | 0 | 0 | `ceil_integers_neg5_10` |
| `clamp` | relu | `torchwright.ops.relu.arithmetic_ops` | 1.907e-06 | 0.0002899 | `clamp_flat_zone_pm20` |
| `compare` | relu | `torchwright.ops.relu.arithmetic_ops` | 1.998 | 1.998 | `compare_near_thresh_0` |
| `cond_gate` | relu | `torchwright.ops.relu.logic_ops` | 0.0009766 | 0.3885 | `cond_gate_signed_bool_times_value` |
| `equals_vector` | relu | `torchwright.ops.relu.logic_ops` | 0 | 0 | `equals_vector_match_and_off` |
| `floor_int` | relu | `torchwright.ops.relu.arithmetic_ops` | 0.9993 | 0.953 | `floor_near_boundary_10` |
| `min` | relu | `torchwright.ops.relu.arithmetic_ops` | 3.815e-06 | 1.953e-06 | `minmax_uniform_pm50` |
| `mod_const` | relu | `torchwright.ops.relu.arithmetic_ops` | 0 | 0 | `mod_integers_0_100_by7` |
| `multiply_2d` | relu | `torchwright.ops.relu.arithmetic_ops` | 0.06249 | 2.15 | `multiply_uniform_pm10` |
| `multiply_integers` | relu | `torchwright.ops.relu.arithmetic_ops` | 0 | 0 | `multiply_integers_0_10` |
| `piecewise_linear` | relu | `torchwright.ops.relu.arithmetic_ops` | 0.25 | 231.1 | `parabola_0_10_step1` |
| `reciprocal` | relu | `torchwright.ops.relu.arithmetic_ops` | 0.0008245 | 0.1361 | `reciprocal_03_200` |
| `square` | relu | `torchwright.ops.relu.arithmetic_ops` | 0.25 | 231.1 | `square_unsigned_0_10` |
| `thermometer_floor_div` | relu | `torchwright.ops.relu.arithmetic_ops` | 0 | 0 | `thermometer_integers_0_100_by10` |
| `abs` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 0.004351 | 0.9954 | `abs_near_zero_pm02` |
| `bool_all_true` | swiglu | `torchwright.ops.swiglu.logic_ops` | 0 | 0 | `bool_triple_signed` |
| `bool_any_true` | swiglu | `torchwright.ops.swiglu.logic_ops` | 0 | 0 | `bool_triple_signed` |
| `bool_not` | swiglu | `torchwright.ops.swiglu.logic_ops` | 0 | 0 | `bool_single_signed` |
| `broadcast_select` | swiglu | `torchwright.ops.swiglu.map_select` | 0 | 0 | `broadcast_select_pm1_masks_4x1` |
| `ceil_int` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 0 | 0 | `ceil_integers_neg5_10` |
| `clamp` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 1.907e-06 | 0.0002899 | `clamp_flat_zone_pm20` |
| `compare` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 1.999 | 1.999 | `compare_near_thresh_0` |
| `cond_gate` | swiglu | `torchwright.ops.swiglu.logic_ops` | 0 | 0 | `cond_gate_signed_bool_times_value` |
| `dynamic_extract` | swiglu | `torchwright.ops.swiglu.map_select` | 0 | 0 | `dynamic_extract_int_idx_4x2` |
| `equals_vector` | swiglu | `torchwright.ops.swiglu.logic_ops` | 0 | 0 | `equals_vector_match_and_off` |
| `floor_int` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 100.7 | 0.9531 | `floor_sawtooth_uniform_0_1023` |
| `in_range` | swiglu | `torchwright.ops.swiglu.map_select` | 0 | 0 | `in_range_integer_bounds_8` |
| `map_to_table` | swiglu | `torchwright.ops.swiglu.map_select` | 0 | 0 | `map_to_table_match_and_off` |
| `min` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 3.815e-06 | 1.953e-06 | `minmax_uniform_pm50` |
| `mod_const` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 0 | 0 | `mod_integers_0_100_by7` |
| `multiply` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 0.0009766 | 2.241e-07 | `multiply_uniform_pm1000` |
| `onehot_lookup` | swiglu | `torchwright.ops.swiglu.onehot_table` | 0 | 0 | `onehot_lookup_two_block` |
| `piecewise_linear` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 0.25 | 146.3 | `parabola_0_10_step1` |
| `radix_floor_int` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 1 | 1 | `radix_floor_lo_ramp` |
| `reciprocal` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 0.0008245 | 0.1117 | `reciprocal_03_200` |
| `select` | swiglu | `torchwright.ops.swiglu.map_select` | 0 | 0 | `select_signed_bool_two_values` |
| `square` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 3.052e-05 | 2.266e-07 | `square_uniform_pm100` |
| `table_lookup_2d` | swiglu | `torchwright.ops.swiglu.map_select` | 0 | 0 | `table_lookup_2d_int_8x6` |
| `thermometer_floor_div` | swiglu | `torchwright.ops.swiglu.arithmetic_ops` | 0 | 0 | `thermometer_integers_0_100_by10` |

## How to read these numbers

Each row reports output noise *at the op's design operating point* —
i.e., with clean inputs drawn from the listed distribution. It does
**not** bound output noise when upstream inputs are themselves noisy.

Per-op bounds are **not additive** through a chain when any op has
internal gain (a constant that multiplies an input). Gate ops
(`cond_gate`, `select`, `attend_mean_where`) and threshold-output ops
(`compare` in its ramp zone, `floor_int` near integer boundaries) all
have structural gains > 1. An upstream deviation of magnitude `1/gain`
can push the gated op outside its published bound, and the bias
propagates multiplicatively downstream. See
`docs/numerical_noise_findings.md` for the worked Phase E example and
the list of known amplification hazards.

To reason about a specific chain, don't try to compose per-op bounds.
Probe the suspect node directly with
`torchwright.debug.probe.probe_compiled`; the residual at the node is
observable, the per-op bounds are an aid for spotting the op *class*
most likely responsible.

## `abs` (relu)

Exact decomposition: `ReLU(x) + ReLU(-x)`. Any non-zero measurement here would indicate a compiler or float32 bug.

### `abs_uniform_pm50`

Uniform signed inputs on [-50, 50]. `abs` is exact (ReLU(x) + ReLU(-x)) so any residual is float32 round-off only.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x=-0.3743` |

## `bool_all_true` (relu)

Single `compare` at threshold `N-1` over the sum of N ±1 inputs. Sum is N only when all are +1; otherwise ≤ N-2 — threshold cleanly separates the two, so exact on ±1 inputs.

### `bool_triple_signed`

Three independent ±1 boolean streams — exercises all 8 truth-table rows of `bool_any_true` and `bool_all_true`.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `a=-1`, `b=1`, `c=1` |

## `bool_any_true` (relu)

Two stacked `compare` layers. On clean ±1 inputs the intermediate sum lands well outside every ramp zone, so the composition is exact.

### `bool_triple_signed`

Three independent ±1 boolean streams — exercises all 8 truth-table rows of `bool_any_true` and `bool_all_true`.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `a=-1`, `b=1`, `c=1` |

## `bool_not` (relu)

Single `compare` with inverted levels. Exact on clean ±1 inputs; inherits `compare`'s ramp-zone error for inputs near 0.

### `bool_single_signed`

Random ±1 inputs (discrete, no ramp-zone samples) — `bool_not` is only defined on clean boolean inputs.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x=-1` |

## `ceil_int` (relu)

Reduction to `-floor_int(-x)`. On integer inputs every value lands in a flat zone of the floor staircase, so the op is exact here even though it inherits approximate semantics for continuous inputs.

### `ceil_integers_neg5_10`

Integer inputs on [-5, 10]. `ceil_int(k) = k` for integer k (flat zones in the floor staircase reused under -floor(-x)).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 3834 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x=7` |

## `clamp` (relu)

Piecewise-linear identity with 4 breakpoints at `[lo, lo + eps, hi - eps, hi]`. In the interior flat zone (this distribution) the op is a pure identity; boundary samples would fall inside the `eps`-wide ramp and lose precision but are out of scope for this negative-control distribution.

### `clamp_flat_zone_pm20`

Uniform samples on [-8, 8] with clamp bounds [-10, 10] — every sample is inside the flat identity zone. Residual measures float32 round-off only.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 1.907e-06 |
| Mean abs error | 2.464e-07 |
| p99 abs error | 9.537e-07 |
| Max rel error | 0.0002899 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 1.99e-07 |
| p99 rel error | 1.661e-06 |
| Worst input | `x=6.46` |

## `compare` (relu)

ReLU-pair ramp. Inputs within `1/step_sharpness` of the threshold land inside the ramp zone and yield an interpolated value; inputs further away match the step function exactly. The `compare_near_thresh_*` distributions deliberately stress the ramp — the reference is a discrete step function, so samples inside the ramp legitimately report large error.

### `compare_near_thresh_0`

Samples within ±0.3 of the threshold at 0.0 — stresses the 2-neuron ramp zone (width 1/step_sharpness).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 1.998 |
| Mean abs error | 0.1679 |
| p99 abs error | 1.901 |
| Max rel error | 1.998 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.1679 |
| p99 rel error | 1.901 |
| Worst input | `x=0.0001239` |

### `compare_uniform_pm80`

Uniform samples on [-80, 80] — measures compare's ramp error well away from the threshold (expected ≈ 0).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 1.339 |
| Mean abs error | 0.0004795 |
| p99 abs error | 0 |
| Max rel error | 1.339 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.0004795 |
| p99 rel error | 0 |
| Worst input | `x=0.03305` |

## `cond_gate` (relu)

`big_offset` ReLU gate. On clean ±1 conditions the output is exactly `inp` (cond=+1) or `0` (cond=-1) up to float32 round-off; noisy conditions blur the gate.

### `cond_gate_signed_bool_times_value`

±1 conditions paired with scalar values in [-5, 5]. Expected output: `value` when cond=+1, `0` when cond=-1.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.0009766 |
| Mean abs error | 0.0002494 |
| p99 abs error | 0.0009595 |
| Max rel error | 0.3885 (over 2089 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.001052 |
| p99 rel error | 0.0116 |
| Worst input | `cond=1`, `inp=3.06` |

## `equals_vector` (relu)

Single-ReLU approximate equality test. Inside the `1/embedding_step_sharpness`-wide transition ball around the target the output interpolates between -1 and +1.

### `equals_vector_match_and_off`

Half exact-match against target vector [1, 2, 3], half random vectors in [-3, 3]³ — measures the ReLU-based equality test's margin of separation between matches and non-matches.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x[0]=1`, `x[1]=2`, `x[2]=3` |

## `floor_int` (relu)

Staircase with ramp width `1/step_sharpness` directly below each integer. Inputs near integer boundaries land in the ramp zone and produce interpolated values; callers with integer-ish inputs should use `thermometer_floor_div` instead, whose ramps sit at half-integer thresholds.

### `floor_near_boundary_10`

Samples within ±1/step_sharpness of integer k=5 — stresses the ramp zone where `floor_int` interpolates.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.9993 |
| Mean abs error | 0.1288 |
| p99 abs error | 0.9704 |
| Max rel error | 0.2498 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.03219 |
| p99 rel error | 0.2426 |
| Worst input | `x=5` |

### `floor_uniform_neg5_10`

Uniform continuous samples on [-5, 10] — most land in flat zones; measures baseline `floor_int` accuracy.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.9982 |
| Mean abs error | 0.04654 |
| p99 abs error | 0.8959 |
| Max rel error | 0.953 (over 3828 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.01849 |
| p99 rel error | 0.4611 |
| Worst input | `x=6` |

## `min` (relu)

Exact via `(a + b - |a - b|) / 2`.

### `minmax_uniform_pm50`

Two independent uniform streams on [-50, 50]². `min`/`max` are exact via the abs-trick; residual is float32 round-off only.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 3.815e-06 |
| Mean abs error | 5.318e-07 |
| p99 abs error | 3.815e-06 |
| Max rel error | 1.953e-06 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 6.585e-08 |
| p99 rel error | 1.953e-06 |
| Worst input | `a=-46.83`, `b=-43.65` |

## `mod_const` (relu)

Identity `x % d = x - d · thermometer_floor_div(x, d)`. Exact on integer inputs (the input class this op is declared for).

### `mod_integers_0_100_by7`

Integer inputs on [0, 100] with divisor=7. `mod_const` is x − divisor × `thermometer_floor_div(x)`, exact on integers.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 3486 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x=64` |

## `multiply_2d` (relu)

Single-sublayer product built analytically via the quarter-square identity `a*b = ((a+b)^2 - (a-b)^2)/4` (O(n) neurons, no least-squares solve). Worst-cell absolute bound is `step1*step2/4 = 0.25` for this configuration.

### `multiply_uniform_pm10`

a·b over [-10, 10]² with step=1 — generic uniform product grid; analytical bound `step1*step2/4 = 0.25`.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.06249 |
| Mean abs error | 0.0202 |
| p99 abs error | 0.05846 |
| Max rel error | 2.15 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.007293 |
| p99 rel error | 0.06618 |
| Worst input | `a=-1.746`, `b=-1.746` |

## `multiply_integers` (relu)

Polarization + `square` on non-negative integer inputs. Exact on integer inputs by construction (the polarization identity is exact; `square` is exact at integer grid points).

### `multiply_integers_0_10`

Integer pairs in [0, 10] — `multiply_integers` is exact on integer inputs by construction; serves as a precision-parity check.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 3387 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `a=10`, `b=2` |

## `piecewise_linear` (relu)

Foundation 1D piecewise-linear primitive. The parabola distribution measures the interpolation configuration (11 integer breakpoints, x²): error is zero at breakpoints and bounded by per-segment interpolation error. The staircase distributions measure the univariate-collapse configuration (docs/univariate_collapse_plan.md): step pairs at half-integers, input_scale = step_sharpness, in-contract inputs — the axis that grows with lane count is fp32 accumulation across the lane sum.

### `parabola_0_10_step1`

f(x)=x² on [0, 10] with integer breakpoints — probes generic `piecewise_linear` precision between grid points.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.25 |
| Mean abs error | 0.1638 |
| p99 abs error | 0.25 |
| Max rel error | 231.1 (over 4094 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.5851 |
| p99 rel error | 11.4 |
| Worst input | `x=6.499` |

### `staircase_2048_lanes`

Integers in [0, 1024] ± the plateau contract band — a 2048-lane staircase (1025 plateaus), past the largest single FFN in production (1,024 lanes) and half the collapse pass's d_hidden/4 decline cap.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.25 |
| Mean abs error | 0.001133 |
| p99 abs error | 0.03125 |
| Max rel error | 0.125 (over 4068 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.0001527 |
| p99 rel error | 0.001311 |
| Worst input | `x=1006` |

### `staircase_512_lanes`

Integers in [0, 256] ± the plateau contract band — a 512-lane staircase (257 plateaus), the thermometer-Attn collapse scale.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.03125 |
| Mean abs error | 0.0002183 |
| p99 abs error | 0.007812 |
| Max rel error | 0.01562 (over 4047 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 1.622e-05 |
| p99 rel error | 0.0005907 |
| Worst input | `x=254` |

### `staircase_64_lanes`

Integers in [0, 32] ± the plateau contract band (±1/(2·step_sharpness)) — in-contract inputs of a 64-lane collapse-synthesized staircase (33 plateaus, every step value-changing).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.0009766 |
| Mean abs error | 1.326e-05 |
| p99 abs error | 0.0002441 |
| Max rel error | 0.0003255 (over 3967 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 1.178e-06 |
| p99 rel error | 1.744e-05 |
| Worst input | `x=26.02` |

## `reciprocal` (relu)

Geometric breakpoint spacing keeps relative interpolation error roughly constant across the range. Measured against a reciprocal graph with `(min_value, max_value, step) = (0.3, 200.0, 1.0)`.

### `reciprocal_03_200`

1/x over [0.3, 200.0], step=1.0.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.0008245 |
| Mean abs error | 0.000114 |
| p99 abs error | 0.0004775 |
| Max rel error | 0.1361 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.01444 |
| p99 rel error | 0.07897 |
| Worst input | `x=0.3159` |

## `square` (relu)

Always an underestimate of x² between grid points. Max segment error is `(step/2)²` at half-integers.

### `square_unsigned_0_10`

x² over [0, 10] with step=1.0 — exact at integers, worst error at half-integers.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.25 |
| Mean abs error | 0.1638 |
| p99 abs error | 0.25 |
| Max rel error | 231.1 (over 4094 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.5851 |
| p99 rel error | 11.4 |
| Worst input | `x=6.499` |

## `thermometer_floor_div` (relu)

Integer-input-only staircase. Ramps sit at `k*divisor - 0.5`, giving clean separation for integer inputs. Feeding continuous floats produces ramp-zone interpolation artefacts — those belong to `floor_int`, not this op.

### `thermometer_integers_0_100_by10`

Integer inputs on [0, 100] with divisor=10 — `thermometer_floor_div` is defined only for integer inputs.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 3699 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x=64` |

## `abs` (swiglu)

`hinge(x) + hinge(-x)` = x·tanh(scale·x/2) — the rare op that regresses under swish (the relu identity is exact; |x| has a corner no smooth lane sum can express). Error is one-sided and bounded: output in [0, |x|], worst underestimate 2·swish_dip/scale (0.0044) at |x| = 1.278/scale; bit-exact for |x| ≳ 0.2 (the whole integer grid).

### `abs_near_zero_pm02`

|x| ≤ 0.2 — the swish abs fillet zone, where the one-sided underestimate peaks (2·swish_dip/scale at |x| = 1.278/scale); the relu abs is exact here.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.004351 |
| Mean abs error | 0.0005043 |
| p99 abs error | 0.004324 |
| Max rel error | 0.9954 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.05772 |
| p99 rel error | 0.9103 |
| Worst input | `x=0.00999` |

### `abs_uniform_pm50`

Uniform signed inputs on [-50, 50]. `abs` is exact (ReLU(x) + ReLU(-x)) so any residual is float32 round-off only.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.003267 |
| Mean abs error | 3.118e-06 |
| p99 abs error | 0 |
| Max rel error | 0.1815 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.0001565 |
| p99 rel error | 0 |
| Worst input | `x=-0.01801` |

## `bool_all_true` (swiglu)

Single swiglu `compare` at threshold `N-1` over the sum of N ±1 inputs — sum is N only when all are +1, otherwise ≤ N-2. Exact to the far-field ulp class on ±1 inputs.

### `bool_triple_signed`

Three independent ±1 boolean streams — exercises all 8 truth-table rows of `bool_any_true` and `bool_all_true`.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `a=-1`, `b=1`, `c=1` |

## `bool_any_true` (swiglu)

Two stacked swiglu `compare` layers. On clean ±1 inputs the intermediate sum lands well outside every ramp zone and fillet; the composition is exact to the stacked far-field ulp class (~1e-6).

### `bool_triple_signed`

Three independent ±1 boolean streams — exercises all 8 truth-table rows of `bool_any_true` and `bool_all_true`.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `a=-1`, `b=1`, `c=1` |

## `bool_not` (swiglu)

Single swiglu `compare` with inverted levels — inherits compare's entry. On ±1 inputs the false side (input +1) is exact and the true side is ulp-class.

### `bool_single_signed`

Random ±1 inputs (discrete, no ramp-zone samples) — `bool_not` is only defined on clean boolean inputs.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x=-1` |

## `broadcast_select` (swiglu)

Per slot, select's complementary gated pair with mask_i as the cond — one form for every caller (the approximate flag, the (M+v)−M offsets, and the two-sublayer exact path all died). Junk-mask safe with no ±1 assert; a mask off ±1 by δ mis-scales the winner by δ·|value|. (The relu broadcast_select was never measured; its offset class is the findings doc's (M+v)−M entry.)

### `broadcast_select_pm1_masks_4x1`

±1 masks over 4 slots with per-slot true/false values in [-5, 5] (d_fill=1). Losing branches exactly zero at clean masks; winners ~1 ulp relative.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 16384 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `masks[0]=-1`, `masks[1]=1`, `masks[2]=1`, `masks[3]=-1`, `t[0]=-1.782`, `t[1]=2.033`, `t[2]=1.573`, `t[3]=-1.13`, `f[0]=1.674`, `f[1]=-2.599`, `f[2]=3.615`, `f[3]=4.378` |

## `ceil_int` (swiglu)

−floor_int(−x); inherits floor_int's entry.

### `ceil_integers_neg5_10`

Integer inputs on [-5, 10]. `ceil_int(k) = k` for integer k (flat zones in the floor staircase reused under -floor(-x)).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 3834 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x=7` |

## `clamp` (swiglu)

piecewise_linear with 4 breakpoints, input_scale = step_sharpness; corners 0.1 apart clear the 34/K spacing audit 3x, so the only additions over relu are two isolated fillets (≤ swish_dip·|Δm|/K each).

### `clamp_flat_zone_pm20`

Uniform samples on [-8, 8] with clamp bounds [-10, 10] — every sample is inside the flat identity zone. Residual measures float32 round-off only.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 1.907e-06 |
| Mean abs error | 2.464e-07 |
| p99 abs error | 9.537e-07 |
| Max rel error | 0.0002899 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 1.99e-07 |
| p99 rel error | 1.661e-06 |
| Worst input | `x=6.46` |

## `compare` (swiglu)

Sharpened-hinge ramp (`hinge(z) − hinge(z−1)`, `hinge(z) = Swish(scale·z)/scale`). Same ramp-zone contract as the ReLU form (in-ramp inputs interpolate; the near-thresh distribution deliberately stresses it). What's new vs relu: fillet dips within ~17/(scale·sharpness) of the two bends can overshoot either level by up to swish_dip/scale·|T−F| (0.0044 total span at scale=128); true-side far-field outputs carry fp32 product rounding at the lane-contribution magnitude — the same class as relu's far-field noise; the false side is exactly out_bias.

### `compare_near_thresh_0`

Samples within ±0.3 of the threshold at 0.0 — stresses the 2-neuron ramp zone (width 1/step_sharpness).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 1.999 |
| Mean abs error | 0.168 |
| p99 abs error | 1.901 |
| Max rel error | 1.999 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.168 |
| p99 rel error | 1.901 |
| Worst input | `x=0.0001239` |

### `compare_uniform_pm80`

Uniform samples on [-80, 80] — measures compare's ramp error well away from the threshold (expected ≈ 0).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 1.339 |
| Mean abs error | 0.0004795 |
| p99 abs error | 0 |
| Max rel error | 1.339 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.0004795 |
| p99 rel error | 0 |
| Worst input | `x=0.03305` |

## `cond_gate` (swiglu)

One gated lane per component: `Swish(scale·cond)·inp/scale`. At clean ±1 conds the off-path is exactly zero in fp32 (σ(−scale) computes as 0.0) and the on-path passes with ~1 ulp relative rounding. No offset M, no finite-range requirement — cond noise lands as δ·|actual value|.

### `cond_gate_signed_bool_times_value`

±1 conditions paired with scalar values in [-5, 5]. Expected output: `value` when cond=+1, `0` when cond=-1.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 2089 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `cond=-1`, `inp=3.637` |

## `dynamic_extract` (swiglu)

in_range one-hot → broadcast_select (zero-literal false branch, lanes dropped) → free Linear sum. Losing slots are exactly zero at clean masks, so the sum degenerates to a copy of the selected slot.

### `dynamic_extract_int_idx_4x2`

Runtime 4×2 tables in [-5, 5] with integer indices — the in_range → broadcast_select → sum composition on clean integer inputs.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 8192 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `table[0]=-0.03743`, `table[1]=2.682`, `table[2]=-4.115`, `table[3]=-3.68`, `table[4]=-1.926`, `table[5]=1.341`, `table[6]=-0.09907`, `table[7]=3.964`, `idx=3` |

## `equals_vector` (swiglu)

One sharpened hinge on the dot-product margin. Matches are bit-exact +1; non-matches within ~17/scale past the 1/speed margin land in the hinge dip and read as low as -1 - 2·swish_dip·speed/scale (-1.0044 at scale=128) — the value-range assert carries that low-side slack. Inside the margin ball the output interpolates, as today.

### `equals_vector_match_and_off`

Half exact-match against target vector [1, 2, 3], half random vectors in [-3, 3]³ — measures the ReLU-based equality test's margin of separation between matches and non-matches.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x[0]=1`, `x[1]=2`, `x[2]=3` |

## `floor_int` (swiglu)

Two chained FFNs (bounded step, then not-yet-ON counting) — the depth is load-bearing exactly as on relu (fp32 2^24 accumulation), and the W-slack absorbs stage-1 fillet noise on ON steps the same way it absorbs fp ulps. Ramp and fillet zones near boundaries interpolate, as today; flat zones and integers are exact to the folded ulp class. The sawtooth distribution measures the `output_map` mode (a following piecewise-constant op folded in by reweighting the stage-2 indicators with per-boundary deltas): reweighting exact 0/1 indicators is exact, but a ramp-zone off-by-one is amplified by the local |delta| — up to -(H-1) = -255 at the wrap boundaries, which dominates this op's footer max.

### `floor_near_boundary_10`

Samples within ±1/step_sharpness of integer k=5 — stresses the ramp zone where `floor_int` interpolates.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.9996 |
| Mean abs error | 0.1288 |
| p99 abs error | 0.971 |
| Max rel error | 0.2499 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.03219 |
| p99 rel error | 0.2428 |
| Worst input | `x=5` |

### `floor_sawtooth_uniform_0_1023`

Uniform continuous samples on [0, 1023] for `floor_int` with `output_map = k % 256` — the sawtooth that folds a following piecewise-constant op into the floor. Most land in flat zones; those inside a ramp just below a multiple of 256 carry the -(H-1)=-255-amplified off-by-one, the worst-case delta.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 100.7 |
| Mean abs error | 0.07575 |
| p99 abs error | 0.8882 |
| Max rel error | 0.7829 (over 4075 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.00177 |
| p99 rel error | 0.02805 |
| Worst input | `x=511.9` |

### `floor_uniform_neg5_10`

Uniform continuous samples on [-5, 10] — most land in flat zones; measures baseline `floor_int` accuracy.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.999 |
| Mean abs error | 0.04655 |
| p99 abs error | 0.8959 |
| Max rel error | 0.9531 (over 3828 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.01849 |
| p99 rel error | 0.4611 |
| Worst input | `x=6` |

## `in_range` (swiglu)

Per slot, two compare-shaped sharpened ramps combined in out_proj. Integer bounds are exact to the folded ulp class; continuous bounds inherit compare's ramp contract per boundary plus up to 4·swish_dip/scale ≈ 0.011 of fillet dip (the value-range assert and broadcast_select's mask tolerance both carry it). (The relu in_range was never measured.)

### `in_range_integer_bounds_8`

Integer (lower, upper) pairs over [0, 8], 8 slots — integer bounds keep every hinge ≥ 4 units from its bend (bit-exact modulo the folded-/scale ulp class).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 32768 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `lower=0`, `upper=8` |

## `map_to_table` (swiglu)

Bank of equals_vector hinges rescaled to 0/1, one degenerate lane per entry, ported hinge-for-hinge. Matches read their value to ~1 ulp (the ×scale/÷scale round trip); no-match indicators underflow, so misses return the default bit-exactly. Between-keys inputs can partially fire several indicators, as today. (The relu map_to_table was never measured.)

### `map_to_table_match_and_off`

Exact key matches (three 3-wide keys) and far-off vectors in [-1, 1]^3 — matches read their value to ~1 ulp, misses return the default bit-exactly (indicators underflow).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 8192 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x[0]=2`, `x[1]=0`, `x[2]=1` |

## `min` (swiglu)

`a − hinge(a−b)` plus a's sharpened bypass pair. One-sided over-estimate ≤ swish_dip/scale (0.0022), only when |a−b| ≲ 0.2; ties exact; far-apart operands carry the folded-/scale product-rounding ulp class.

### `minmax_uniform_pm50`

Two independent uniform streams on [-50, 50]². `min`/`max` are exact via the abs-trick; residual is float32 round-off only.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 3.815e-06 |
| Mean abs error | 2.77e-07 |
| p99 abs error | 3.815e-06 |
| Max rel error | 1.953e-06 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 3.296e-08 |
| p99 rel error | 4.506e-07 |
| Worst input | `a=-8.73`, `b=-46.83` |

## `mod_const` (swiglu)

x − d·thermometer_floor_div(x, d) — linear hardware plus the staircase; exact on integer inputs to the folded ulp class, as on relu.

### `mod_integers_0_100_by7`

Integer inputs on [0, 100] with divisor=7. `mod_const` is x − divisor × `thermometer_floor_div(x)`, exact on integers.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 3486 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x=64` |

## `multiply` (swiglu)

Exact ± gated-lane pair: `Swish(a)·b + Swish(-a)·(-b) = a·b` (σ(a) + σ(-a) = 1). Exact in real math at any magnitude — no grid, no range limit; measured error is fp32 rounding, relative to the actual product. Replaces the quarter-square `multiply_2d` and the `multiply_integers` chain.

### `multiply_uniform_pm10`

a·b over [-10, 10]² with step=1 — generic uniform product grid; analytical bound `step1*step2/4 = 0.25`.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 1.526e-05 |
| Mean abs error | 1.177e-06 |
| p99 abs error | 7.629e-06 |
| Max rel error | 2.241e-07 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 4.039e-08 |
| p99 rel error | 1.68e-07 |
| Worst input | `a=-8.413`, `b=-9.365` |

### `multiply_uniform_pm1000`

a·b over [-1000, 1000]² — magnitudes far beyond any ReLU-era grid; probes the range-free claim of the exact gated product.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.0009766 |
| Mean abs error | 8.106e-06 |
| p99 abs error | 0 |
| Max rel error | 9.938e-08 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 7.319e-10 |
| p99 rel error | 0 |
| Worst input | `a=-15.87`, `b=-873` |

## `onehot_lookup` (swiglu)

Exact integer block counting: the -(n_blocks - 0.5) bias parks the winner's sharpened hinge argument at +scale/2 and everyone else's at ≤ -scale/2. Winner indicator is exactly 0.5 in fp32; losing lanes leak hinge(-0.5) ≈ -1e-28 — invisible. The tight [min, max] range claim is the reason this op exists. (The relu onehot_lookup was never measured.)

### `onehot_lookup_two_block`

digit(3) ⊕ carry(2) one-hot pairs over all six combinations; three are table rows, three miss → default.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x[0]=0`, `x[1]=0`, `x[2]=1`, `x[3]=1`, `x[4]=0` |

## `piecewise_linear` (swiglu)

The relu construction with sharpened hinges (K = scale·input_scale in the gate rows). Chord error between breakpoints is untouched and dominates, exactly as on relu; each corner adds a radius-17/K fillet whose dip (≤ swish_dip·|Δm|/K) bends toward the curve — grids with spacing under 34/K must raise input_scale (the spacing audit; reciprocal and the BOS inversion table do). The staircase distributions measure the univariate-collapse configuration (step pairs 1/step_sharpness apart at input_scale = step_sharpness, which clears the spacing audit; in-contract inputs sit ≥ 0.4 from every hinge, so fillet tails underflow and the measured axis is fp32 lane-sum accumulation, as on relu).

### `parabola_0_10_step1`

f(x)=x² on [0, 10] with integer breakpoints — probes generic `piecewise_linear` precision between grid points.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.25 |
| Mean abs error | 0.1637 |
| p99 abs error | 0.25 |
| Max rel error | 146.3 (over 4094 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.5284 |
| p99 rel error | 11.4 |
| Worst input | `x=6.499` |

### `staircase_2048_lanes`

Integers in [0, 1024] ± the plateau contract band — a 2048-lane staircase (1025 plateaus), past the largest single FFN in production (1,024 lanes) and half the collapse pass's d_hidden/4 decline cap.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.25 |
| Mean abs error | 0.001133 |
| p99 abs error | 0.03125 |
| Max rel error | 0.125 (over 4068 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.0001527 |
| p99 rel error | 0.001311 |
| Worst input | `x=1006` |

### `staircase_512_lanes`

Integers in [0, 256] ± the plateau contract band — a 512-lane staircase (257 plateaus), the thermometer-Attn collapse scale.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.03125 |
| Mean abs error | 0.0002183 |
| p99 abs error | 0.007812 |
| Max rel error | 0.01562 (over 4047 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 1.622e-05 |
| p99 rel error | 0.0005907 |
| Worst input | `x=254` |

### `staircase_64_lanes`

Integers in [0, 32] ± the plateau contract band (±1/(2·step_sharpness)) — in-contract inputs of a 64-lane collapse-synthesized staircase (33 plateaus, every step value-changing).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.0009766 |
| Mean abs error | 1.326e-05 |
| p99 abs error | 0.0002441 |
| Max rel error | 0.0003255 (over 3967 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 1.178e-06 |
| p99 rel error | 1.744e-05 |
| Worst input | `x=26.02` |

## `radix_floor_int` (swiglu)

floor(x) as hi-floor -> integer snap -> lo-floor over [-1, D] (D = power of two nearest sqrt(2N)); ~8.5*sqrt(N) lanes vs 3N flat, intermediates ~sqrt(N) wide. The snapped hi and the extended lo range make the divisor-boundary sliver reconstruct exactly (no D-amplification); the LO ramp keeps flat floor_int's tolerated ±1-step window. Measured at the DOOM native-coordinate configuration (N=2046, s=10^4, D=64).

### `radix_floor_divisor_sliver`

Samples just below multiples of the divisor D=64 (offsets log-uniform in [1.2e-4, 6.3e-3]): inside the HI floor's ramp but outside the LO floor's — the D-amplification hazard the integer snap neutralizes. Expected ≈ exact.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x=192` |

### `radix_floor_lo_ramp`

Samples within 1/sharpness below random integers in [-1023, 1023] — inside the LO ramp, the same tolerated interpolation window as flat `floor_int` (error up to 1 step, never D-amplified).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 1 |
| Mean abs error | 0.2666 |
| p99 abs error | 1 |
| Max rel error | 1 (over 4093 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.005008 |
| p99 rel error | 0.09406 |
| Worst input | `x=217` |

### `radix_floor_native_uniform`

Uniform continuous samples on [-1023, 1023] — the DOOM native texture-coordinate range (`render_ops.FLOOR_NATIVE`, the production `radix_floor_int` call site, sharpness=10^4).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 1 |
| Mean abs error | 0.0002441 |
| p99 abs error | 0 |
| Max rel error | 0.002924 (over 4094 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 7.142e-07 |
| p99 rel error | 0 |
| Worst input | `x=-341` |

## `reciprocal` (swiglu)

Geometric grid; the smallest gap (at min_value) fails the 34/K spacing audit at input_scale=1 (stacked fillets reached ~2e-2, a 20x regression), so the op derives input_scale from the smallest gap — with the fillets separated, the measured error is the chord class, as on relu.

### `reciprocal_03_200`

1/x over [0.3, 200.0], step=1.0.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0.0008245 |
| Mean abs error | 0.0001067 |
| p99 abs error | 0.0004489 |
| Max rel error | 0.1117 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0.01334 |
| p99 rel error | 0.07525 |
| Worst input | `x=0.3159` |

## `select` (swiglu)

Complementary gated pair: `Swish(scale·cond)·a/scale + Swish(−scale·cond)·b/scale`. The losing branch contributes exactly zero at clean conds; the winner passes with ~1 ulp relative rounding. (The relu-machine select was never measured — its additive-cancellation error class, half an ulp of M, is documented in the findings doc instead.)

### `select_signed_bool_two_values`

±1 conditions paired with two independent branch values in [-5, 5]. Expected output: `a` when cond=+1, `b` when cond=-1.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `cond=-1`, `a=3.637`, `b=-0.1667` |

## `square` (swiglu)

`multiply` with both operands the same input: `Swish(x)·x + Swish(-x)·(-x) = x²`, both terms `x²·σ(±x)` ≥ 0. Exact in real math for all x — no [0, max_value] contract, no grid, and none of the piecewise version's near-zero relative-error blowup.

### `square_uniform_pm100`

x² over [-100, 100] — signed and wide; probes the range-free claim of the exact gated square (no [0, max_value] contract).

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 3.052e-05 |
| Mean abs error | 6.02e-07 |
| p99 abs error | 1.526e-05 |
| Max rel error | 2.119e-07 (over 4096 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 6.91e-09 |
| p99 rel error | 1.111e-07 |
| Worst input | `x=15.01` |

### `square_unsigned_0_10`

x² over [0, 10] with step=1.0 — exact at integers, worst error at half-integers.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 1.526e-05 |
| Mean abs error | 1.362e-06 |
| p99 abs error | 7.629e-06 |
| Max rel error | 2.266e-07 (over 4094 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 3.972e-08 |
| p99 rel error | 1.468e-07 |
| Worst input | `x=8.69` |

## `table_lookup_2d` (swiglu)

Both axes floor_int's two-stage staircase; the column stage is one gated lane per boundary (up row = the live row vector's adjacent difference) — the ReLU machine's mask staircase and offset-cancellation gate collapse into it, deleting the offset·0.005 guard term (the family's last range-coupled offset). On the integer grid every indicator is exactly 0/1, so the error is the delta telescoping's fp32 accumulation. (The relu table_lookup_2d was never measured; its offset-gate error class is the (M+v)−M findings entry.)

### `table_lookup_2d_int_8x6`

Integer (i, j) over an 8×6 table with 25%% out-of-range (clamped) indices — every indicator saturated, the value path is the delta telescoping's fp32 accumulation.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 3869 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `i=6`, `j=-2` |

## `thermometer_floor_div` (swiglu)

Integer-input-only staircase via piecewise_linear at input_scale = step_sharpness. Integer inputs sit in flat zones ≥ 4 units from every ramp — saturated, so exact to the folded ulp class.

### `thermometer_integers_0_100_by10`

Integer inputs on [0, 100] with divisor=10 — `thermometer_floor_div` is defined only for integer inputs.

| Metric | Value |
| --- | --- |
| Samples | 4096 |
| Max abs error | 0 |
| Mean abs error | 0 |
| p99 abs error | 0 |
| Max rel error | 0 (over 3699 samples with &#124;ref&#124; ≥ 1e-6) |
| Mean rel error | 0 |
| p99 rel error | 0 |
| Worst input | `x=64` |

## Methodology

Measurements run through `torchwright.debug.noise.measure_op_isolated`,
which builds a minimal graph around the op and evaluates it via
`Node.compute` — the same oracle path the compiler already trusts as
the semantic definition of each node. The piecewise-linear approximation
error is the difference between that oracle evaluation and the exact-math
reference function declared in `_target_ops` at
`scripts/measure_op_noise.py`.

All runs are CPU-only and seeded so measurements are bit-identical across
machines at a given commit; the consistency test in `tests/docs/` relies
on that. Floats in the JSON are rounded to 6 significant figures; the
markdown and docstring footers display `.4g` formatting.

**Relative error** is reported alongside absolute error because absolute
numbers are misleading for ops whose output magnitude varies over orders of
magnitude (reciprocal, multiplication, low-rank lookups). Per-sample relative
error is `|compiled - reference| / |reference|`, computed only where
`|reference| >= 1e-6` — samples with a near-zero reference are excluded
(rel error is ill-defined there) and the count of contributing samples is
reported as `rel_valid_samples`. Ops whose reference is always near zero
show `n/a` for relative error.

## Adding a new op

1. In `scripts/measure_op_noise.py`, add any new `InputDistribution` to
   `_distributions()` with a production-grounded name.
2. Append a `TargetOp(...)` to `_target_ops()` wiring `build_graph`,
   `reference_fn`, `input_specs`, and `distribution_names`.
3. Run `make measure-noise`. Commit the diff in `docs/`,
   `torchwright/ops/`, and nothing else.
